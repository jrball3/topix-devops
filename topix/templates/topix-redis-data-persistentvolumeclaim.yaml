apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    topix.service: topix-redis-data
  name: topix-redis-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
